Sections::

Upper: The top section with some hot keys and commands that are universally
useable. Currently has the bindmount with a browse button. 


Panel 1: Submodules

This panel will be a quick way to pull and setup the submodules.
There should be an indication as to which submodules are installed inside Helmsman, and a button to init each submodule