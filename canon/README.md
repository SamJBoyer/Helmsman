c_hDocs contains the canon representations of each hDoc in this tagged version.
instructions/ contains files with agent-ready instructions for performing certain tasks 

both c_hDocs and instructions are inside canon because both are strictly related to regulating the helmsman project and can't be allowed to drift
hSkills will often make use of canon using a junction, so they always pull the contents of canon. This should allow us to keep canon as a single
source of truth.